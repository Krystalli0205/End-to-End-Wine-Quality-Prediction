pandas 
notebook
numpy
scikit-learn
matplotlib
python-box==6.0.2
pyYAML
tqdm
ensure==1.0.2
joblib
types-PyYAML
Flask
Flask-Cors
-e . # used to install the project in "editable" or "development" mode, 
    # meaning any changes made to the source code are immediately reflected without needing to reinstall the package.